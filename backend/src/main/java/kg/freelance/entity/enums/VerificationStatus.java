package kg.freelance.entity.enums;

public enum VerificationStatus {
    NONE,       // No verification request
    PENDING,    // Waiting for admin review
    APPROVED,   // Verification approved
    REJECTED    // Verification rejected
}
